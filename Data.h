#include <iostream>
#include <fstream>
#include <cmath>
#include <chrono>

using namespace std;
using namespace chrono;
template <class T>
extern void HoanVi(T& a, T& b);
extern void GenerateRandomData(int a[], int n);
extern void GenerateSortedData(int a[], int n);
extern void GenerateReverseData(int a[], int n);
extern void GenerateNearlySortedData(int a[], int n);
extern void GenerateData(int a[], int n, int dataType);

